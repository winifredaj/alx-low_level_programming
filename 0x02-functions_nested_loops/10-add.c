#include <stdio.h>
#include "main.h"
/**
 * add- adds two integers 
 *
 * @x: characters to be added
 *
 * @y: integer to add
 *
 * Return: results of adddition
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
