#include <stdio.h>
#include "main.h"
/**
 * add -adds two integers 
 *
 * @x,y: characters to be added
 *
 *Return: results of adddition
 */
int add(int, int)
{
	int x, y;

	x = x + y;
	return (x);
}
