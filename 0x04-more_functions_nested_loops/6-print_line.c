#include "main.h"

/**
 * print_line - check the code
 * @n: number of lines to print
 * Return: Always 0.
 */
void print_line(int n)
{
	for (n = 0; n <= n; n++)
	{
		if(n > 0)
			putchar(_);
			putchar('\n')
		else
			putchar('\n');
	}
}
