#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main -prints function
 *
 * Return: zero on success
 *
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d\n", num);
	}
	return (0);
}
